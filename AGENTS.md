# Suzhou Campus Hub 长期开发规则

- 保持 Flask Application Factory 和蓝图模块化结构。
- 所有新增功能必须有与业务相符的权限校验。
- 所有用户输入必须在服务端验证，输出依赖 Jinja 自动转义。
- 不采集不必要的敏感信息，不记录密码、联系方式等敏感数据。
- 不使用学校校徽或官方视觉规范，始终明确平台的非官方身份。
- 所有重要功能必须补充自动化测试。
- 优先保证移动端体验，同时支持桌面端。
- 代码、路由内部名和数据库字段使用英文。
- 用户界面使用简体中文，不使用无意义的英文占位文案。
- 不得删除已有功能来规避错误，应定位原因并修复。
- 调查默认最小化收集个人信息，不得保存完整 IP 或进行设备指纹追踪。
- 调查答案、统计和导出只能按角色授权访问，公开页面不得暴露其他答卷。
- 调查结构或答卷模型变更必须创建数据库迁移，并补充防重复和验证测试。
- 商业化或家教前台能力必须受功能开关控制；默认关闭时仅保留数据、低调申请入口和管理员管理能力。
- 公网部署不等于内容公开；校内帖子、指南、地图和学习资源必须在服务端校验登录与学生认证。
- 生产环境必须使用 PostgreSQL、强密钥、HTTPS 反向代理和 Secure Cookie，不得使用 Flask 开发服务器。
- 每次生产数据库迁移前必须完成可验证备份，并定期执行隔离恢复演练。
- 环境变量只通过受限环境文件或密钥管理注入，不得提交真实 `.env`、数据库密码或 API 密钥。
